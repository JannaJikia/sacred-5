/** Completions counted across all practices for the day before this reward fires. */
export const DAILY_COMPLETION_GOAL = 5;

export const MILESTONE_REWARD_COINS = 10;

export const REWARD_KEY_FIVE_COMPLETIONS = "FIVE_COMPLETIONS" as const;

/**
 * Coin costs for upcoming redemptions. Shown as savings targets today (coins are
 * earned free); redemption is wired up when the spend economy ships.
 */
export const STREAK_FREEZE_COST = 50;
export const THEME_PACK_COST = 120;
